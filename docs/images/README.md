# Screenshots

Drop the files here with exactly these names and the README picks them up.
Nothing else needs editing.

| File | What to capture |
|---|---|
| `ring.png` | The ring on a dashboard, idle. The hero shot - make it a wide one. |
| `build.png` | Build mode open, ideally with a real session in the panel. |
| `setup.png` | The "Name your assistant" screen, with both sections collapsed. |
| `editor.png` | The card editor, scrolled so the animation dropdown is visible. |
| `timers.png` | The timers card with two or three running. |

A dark-theme dashboard suits the cards better than light. Crop to the card
rather than the whole browser, except for `ring.png` - the point of that one
is how it sits in a room's dashboard.

These are referenced from the top-level README, so a missing file shows as a
broken image. Add them all or trim the block that references them.
